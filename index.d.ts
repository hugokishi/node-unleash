/**
  * ! Use this function if you want to export the logs and warn of a possible error
  */
export function isEnabled(flagName: string, context: Unleash.Context): boolean

/**
 * ! Start a new unleash client
 */
export function InitUnleashClient(): void
